# Offline inventory used only for `make syntax-check`. No real hosts.
[webservers]
web-test ansible_host=127.0.0.1

[dbservers]
db-test ansible_host=127.0.0.1

[all:vars]
ansible_connection=local
env=test
