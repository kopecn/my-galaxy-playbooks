---
# Lowest-precedence, user-overridable variables. Non-secret 1Password
# coordinates; the actual credential is resolved from the vault at run time.
# Override per environment in the downstream inventory group_vars.
onePasswordVault: Personal-Automation
onePasswordTailscaleAPIKey: Tailscale Auth Key - Ansible Provisioning
